# Prisma automatically loads the .env file that sits next to schema.prisma,
# so this lets `prisma` CLI commands (generate/migrate/studio) work when run
# directly from database/, independent of backend/.env. Copy to database/.env
# and point it at the same database backend/.env uses.

DATABASE_URL=postgresql://user:password@localhost:5432/deze
