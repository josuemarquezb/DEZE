// controllers/auth.controller.js — business logic for authentication (placeholder).
// Future exports: register, login, refreshToken, logout, etc.
// Controllers stay thin — validation lives in middleware, DB access via config/db.js.

export const placeholder = (req, res) => {
  res.status(501).json({ message: 'Not implemented yet' });
};
